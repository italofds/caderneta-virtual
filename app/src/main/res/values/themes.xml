<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base theme; Compose supplies the real Material 3 color scheme at runtime. -->
    <style name="Theme.CadernetaVirtual" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>
</resources>
